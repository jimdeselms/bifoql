(aws ecr describe-images --repository-name bifoql --query "imageDetails[].imageTags[]" | convertfrom-json) | ? { $_.EndsWith('-staging') } | % { [int]($_ -replace '-staging', '') } | sort -descending | select-object -first 1
